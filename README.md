# databricks-data-lineage-replication
Metadata-driven data replication and lineage framework built using Azure Databricks. The project replicates raw data from multiple source systems into a centralized replication zone, validates data consistency, and enables scalable dataset onboarding through GitHub-managed metadata.
